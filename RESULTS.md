# 測定結果

**測定は1実装につき1回だけ** (`scripts/measure.sh`、やり直しなし)。
章をまたぐ数字は測定日が異なるため、比較するときはその旨を明示する。

## 測定条件 (全実装共通)

- Windows 11 / Node v25.2.1 / npm (すべての実装で同一マシン・同一バージョン)
- 測定の直前に動作確認 (install + build + 画面操作) を1度行う。
  したがって **npm キャッシュは全実装とも温まった状態**で測る (条件を揃えるため)
- `install` はクリーンな状態 (node_modules / dist 削除後) から
- `dev-until-http200` は起動コマンド実行から HTTP 200 が返るまでの壁時計。
  ツール自身が出す "ready in" 表示は使わない
- JS 転送量は `gzip -c <ファイル>` で数える (全実装同じ)。
  ルートごとにチャンクを分ける実装 (第5回 Next.js 以降) では
  「ビルド成果物の JS 合計」と「初回表示で読む JS」が一致しないので両方を載せる。
  **章をまたいで比べるときは「初回表示で読む JS」を使う**

## 第1章: 土台

### vite (vanilla-ts) — 測定日 2026-08-01

| 指標 | 値 |
|---|---|
| install (キャッシュ温) | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 45 |
| 本番ビルド (`tsc && vite build`) | 1,491 ms |
| JS 転送量 (gzip 合計) | **2,479 B** |
| dev サーバ起動 → HTTP 200 | 1,177 ms |
| 公式スキャフォールドの行数 (vanilla-ts) | 417 行 |
| 共通デモの実装行数 (main.ts + index.html) | 120 行 |

備考:

- フレームワークなしの**基準値**。JS 2.5KB は「この画面を素で書くとこの重さ」という下限で、
  以降の各フレームワークの数字はこれとの差分が「フレームワーク本体の重さ」になる
- 素の実装では、入力欄を作り直すとフォーカスが飛ぶため「骨組みは一度だけ描き、
  変わる部分だけ書き換える」処理を自分で書く必要があった (main.ts 参照)。
  この手間が、以降の回でフレームワークが解決する問題そのもの

## 第2章: React 以外の三択

### vue (Vue 3 / create-vue --typescript) — 測定日 2026-08-01

| 指標 | 値 | 基準値 (vite vanilla-ts) |
|---|---|---|
| install (キャッシュ温) | 2,036 ms | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 179 | 45 |
| 本番ビルド (`vue-tsc && vite build`) | 1,839 ms | 1,491 ms |
| JS 転送量 (gzip 合計) | **26,345 B** | 2,479 B |
| dev サーバ起動 → HTTP 200 | 1,146 ms | 1,177 ms |
| 公式スキャフォールドの行数 | 573 行 | 417 行 |
| 共通デモの実装行数 | **102 行** | 120 行 |

備考:

- **+約24KB (gzip) が Vue 本体の重さ**。引き換えに、素の版で自分で書いていた画面更新処理
  (update関数・フォーカス管理) が丸ごと消え、実装は102行に減った
- 開発サーバの起動は基準値とほぼ同じ (どちらも Vite の上に乗っているため)
- ビルドが遅いのは型検査 (vue-tsc) を含むため。スキャフォールドの build スクリプトのまま測った
- 特徴デモ (`vue/specialty.html`) は測定後に追加した別エントリで、**測定値には含まれない**
  (index エントリのバンドルからは参照されない)

### angular (Angular CLI `ng new --defaults`) — 測定日 2026-08-01

| 指標 | 値 | Vue 3 | 基準値 (素のTS) |
|---|---|---|---|
| install (キャッシュ温) | 8,460 ms | 2,036 ms | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 496 | 179 | 45 |
| 本番ビルド (`ng build`) | 2,413 ms | 1,839 ms | 1,491 ms |
| JS 転送量 (gzip 合計) | **65,271 B** | 26,345 B | 2,479 B |
| dev サーバ起動 → HTTP 200 (`ng serve`) | 2,168 ms | 1,146 ms | 1,177 ms |
| 公式スキャフォールドの行数 (lockfile 除く) | 642 行 | 573 行 | 417 行 |
| 共通デモの実装行数 | 112 行 | 102 行 | 120 行 |

備考:

- 公式既定 (`--defaults`) のまま測った。**既定でルータが入る**のが Angular らしさで、
  未使用でも構成に含まれる (全部入りの設計思想)。外さずにそのまま測定した
- 実装は standalone コンポーネント + signals (現行公式のモダン記法)
- 特徴デモ (型付きリアクティブフォーム、`/specialty`) は**測定後に追加**。
  動的 import の別チャンクで、共通デモ側の測定値は追加前のものが正
- dev サーバは Vite 系ではなく Angular CLI (`ng serve`)。それでも 2.2 秒で表示された

### svelte (Svelte 5 / vite svelte-ts テンプレート) — 測定日 2026-08-01

| 指標 | 値 | Angular | Vue 3 | 基準値 (素のTS) |
|---|---|---|---|---|
| install (キャッシュ温) | 1,268 ms | 8,460 | 2,036 | 1,031 |
| 依存パッケージ数 | **78** | 496 | 179 | 45 |
| 本番ビルド | **1,180 ms** | 2,413 | 1,839 | 1,491 |
| JS 転送量 (gzip 合計) | **15,502 B** | 65,271 | 26,345 | 2,479 |
| dev サーバ起動 → HTTP 200 | 1,701 ms | 2,168 | 1,146 | 1,177 |
| 公式スキャフォールドの行数 | 499 行 | 642 | 573 | 417 |
| 共通デモの実装行数 | **100 行** | 112 | 102 | 120 |

備考:

- **三択で最軽量 (15.5KB)・最少依存 (78)・最速ビルド (1.2秒)・最少実装 (100行)**。
  コンパイル時にフレームワークの仕事を済ませ、実行時の本体をほぼ持たない設計の効果が
  そのまま数字に出た。本番ビルドが素のTS実装 (tsc 込み 1.5秒) より速いのも同じ理由
- 実装は Svelte 5 の runes ($state / $derived)。現行公式のモダン記法
- スキャフォールドは `npm create vite@latest -- --template svelte-ts` (単体 Svelte の公式経路)。
  Svelte 公式 CLI (`sv create`) は SvelteKit を作るため第7回で扱う
- 特徴デモ (`svelte/specialty.html`、animate:flip + transition:fly) は**測定後に追加**の別エントリ

## 第3章: フルスタック版

### nextjs (Next.js 16 / App Router) — 測定日 2026-08-02

| 指標 | 値 | Svelte | Angular | Vue 3 | 基準値 (素のTS) |
|---|---|---|---|---|---|
| install (キャッシュ温) | 12,015 ms | 1,268 | 8,460 | 2,036 | 1,031 |
| 依存パッケージ数 | 58 | 78 | 496 | 179 | 45 |
| 本番ビルド | 4,493 ms | 1,180 | 2,413 | 1,839 | 1,491 |
| **初回表示で読む JS (gzip)** | **147,671 B** | 15,502 | 65,271 | 26,345 | 2,479 |
| ビルド成果物の JS 合計 (gzip) | 187,511 B | 15,502 | 65,271 | 26,345 | 2,479 |
| dev サーバ起動 → HTTP 200 | 2,463 ms | 1,701 | 2,168 | 1,146 | 1,177 |
| 公式スキャフォールドの行数 | 432 行 | 499 | 642 | 573 | 417 |
| 共通デモの実装行数 | 120 行 | 100 | 112 | 102 | 120 |

備考:

- **JS 転送量の行が2つあるのは Next.js だけルートごとにチャンクを分けるため**。
  第1〜4回の実装は単一バンドルなので「ビルド成果物の JS 合計」＝「初回に読む量」だが、
  Next.js は一致しない (合計 187,511 B のうち `/` が読むのは 6 ファイル・147,671 B)。
  **他実装と比べるときは「初回表示で読む JS」の行を使う**。前者だけを並べると
  読み込みもしないチャンクを数に入れることになる
- 初回に読む 6 ファイルは**実ブラウザ (Playwright) の読み込み実測で確定**した。
  HTML の `src=` を拾う方式は、Next.js が HTML に埋め込む RSC ペイロード内の
  チャンクパスまで拾って 1 ファイル多く数える (39,479 B 過大になった)
- gzip の数え方は全実装で `gzip -c <ファイル>` に統一 (measure.sh と同じ)
- `next start` は既定でこれらを圧縮せずに返す。無圧縮の実転送は 519,356 B。
  実運用では配信側で圧縮するのが前提の値
- スキャフォールドは
  `npx create-next-app@latest nextjs --ts --app --no-tailwind --no-eslint --no-src-dir --import-alias "@/*" --use-npm --disable-git --no-agents-md --yes`。
  **既定の Tailwind を外している** — 入れると共通 CSS と衝突して見た目が揃わなくなるため。
  行数は lockfile・node_modules・生成物 (`.next/`)・バイナリを除いた数
- 共通 CSS とデータをリポジトリ直下から読むため `next.config.ts` に `turbopack.root` を追加
  (Turbopack は既定でプロジェクトより上を解決しない)。実装行数には数えていない
  (他実装も雛形の設定ファイルは数えていないため)
- dev の左下に出る Next.js バッジは `devIndicators: false` で消した。
  他実装には出ないので、出したままだと「見た目当てクイズ」が成立しない
- **`dev サーバ起動 → HTTP 200` は他実装と意味が少し違う**。App Router の `/` は
  サーバ側で描いてから返すので、ルートのコンパイルが終わるまで 200 が返らない。
  Vite 系は中身の変換前に HTML の外枠を返す。数字を並べるときはこの差を明示する
- スキャフォールド直後の `npm install` が high 3 件の脆弱性を報告した (`npm audit`)

### nuxt (Nuxt 4 / minimal テンプレート) — 測定日 2026-08-02

| 指標 | 値 | Next.js | Svelte | Vue 3 | 基準値 (素のTS) |
|---|---|---|---|---|---|
| install (キャッシュ温) | 6,377 ms | 12,015 | 1,268 | 2,036 | 1,031 |
| 依存パッケージ数 | **703** | 58 | 78 | 179 | 45 |
| 本番ビルド | 5,499 ms | 4,493 | 1,180 | 1,839 | 1,491 |
| **初回表示で読む JS (gzip)** | **54,723 B** | 147,671 | 15,502 | 26,345 | 2,479 |
| ビルド成果物の JS 合計 (gzip) | 58,723 B | 187,511 | 15,502 | 26,345 | 2,479 |
| dev サーバ起動 → HTTP 200 | 3,831 ms | 2,463 | 1,701 | 1,146 | 1,177 |
| 公式スキャフォールドの行数 | **147 行** | 432 | 499 | 573 | 417 |
| 共通デモの実装行数 | **86 行** | 120 | 100 | 102 | 120 |

備考:

- **同じフルスタック版でも初回に読む JS は Next.js の約 1/2.7** (54.7KB 対 147.7KB)。
  中身は Vue 3 なので、第2回の Vue 単体 26.3KB に対しては約 2.1 倍。
  その差がフレームワークの枠組み (ルーティング・サーバ連携・事前レンダリング) のぶん
- **依存パッケージ 703 はシリーズ最多** (これまでの最多は Angular の 496)。
  一方でインストールは 6.4 秒で Next.js の 12.0 秒より速い。
  **パッケージ数とダウンロードの重さは別物**という第5回の観察が、ここでは裏返しの形で出た
- スキャフォールド 147 行・実装 86 行はどちらもシリーズ最少。
  HTML シェルもエントリファイルも書かないため (`app/app.vue` 1枚で済む)
- 初回に読む 4 ファイルは実ブラウザ (Playwright) の読み込み実測で確定。
  ビルド成果物にはこれに加えてエラーページ用チャンク (4,000 B) がある
- スキャフォールドは `npx nuxi@latest init nuxt -t minimal --packageManager npm --no-gitInit`。
  非対話実行では `--template` が必須。`minimal` は素の Nuxt スターター
  (`content` / `ui` は用途別の別テンプレート)
- 共通 CSS とデータをリポジトリ直下から読むため `nuxt.config.ts` に
  `vite.server.fs.allow: ['..']` を追加。実装行数には数えていない
- dev の Nuxt DevTools バッジは `devtools: { enabled: false }` で消した
  (他実装には出ないので「見た目当てクイズ」が成立しなくなる)
- 特徴デモ (`app/pages/specialty.vue` + `server/api/repos.get.ts`) は**測定後に追加**。
  追加時に共通デモを `app/app.vue` から `app/pages/index.vue` へ移している
  (ファイルベースルーティングを見せるため)。画面の中身は測定時のまま

### sveltekit (SvelteKit 2 / sv create minimal) — 測定日 2026-08-02

| 指標 | 値 | Nuxt | Next.js | Svelte | 基準値 (素のTS) |
|---|---|---|---|---|---|
| install (キャッシュ温) | 2,244 ms | 6,377 | 12,015 | 1,268 | 1,031 |
| 依存パッケージ数 | 86 | 703 | 58 | 78 | 45 |
| 本番ビルド | 2,413 ms | 5,499 | 4,493 | 1,180 | 1,491 |
| **初回表示で読む JS (gzip)** | **32,537 B** | 54,723 | 147,671 | 15,502 | 2,479 |
| ビルド成果物の JS 合計 (gzip) | 33,209 B | 58,723 | 187,511 | 15,502 | 2,479 |
| dev サーバ起動 → HTTP 200 | 1,928 ms | 3,831 | 2,463 | 1,701 | 1,177 |
| 公式スキャフォールドの行数 | 174 行 | 147 | 432 | 499 | 417 |
| 共通デモの実装行数 | 94 行 | 86 | 120 | 100 | 120 |

備考:

- **フルスタック3種で最軽量 (32.5KB)**。Nuxt の約6割、Next.js の約1/4.5。
  実行時ライブラリを持たないコンパイラ方式 (第4回) の効果がフルスタックでも残る
- **「その周り」の倍率はビュー系と同じ約2.1倍**: Svelte 単体 15.5KB → SvelteKit 32.5KB (2.10倍)。
  Vue 26.3KB → Nuxt 54.7KB も 2.08倍で、フレームワークの枠組みの重さがほぼ同率という
  面白い一致が出た
- 初回に読む 8 ファイルは実ブラウザ (Playwright) の読み込み実測で確定。
  ビルド成果物との差は 672 B (未読込チャンク 1 本)
- スキャフォールドは `npx sv create sveltekit --template minimal --types ts --no-add-ons --install npm`。
  第4回の単体 Svelte は vite テンプレート経由だったが、SvelteKit は Svelte 公式 CLI (`sv`) が正規経路。
  現行の雛形は `svelte.config.js` を持たず、設定は `vite.config.ts` に統合されている
- 共通 CSS とデータをリポジトリ直下から読むため `vite.config.ts` に
  `server.fs.allow: ['..']` を追加 (Nuxt と同じ対応)。実装行数には数えていない
- 実装 94 行 = `+page.svelte` 81 + `+layout.svelte` 13。中身は第4回の App.svelte とほぼ同一で、
  HTML シェルとエントリファイルが不要になったぶん短い
- adapter-auto はこの環境でデプロイ先を検出できず警告を出すが、build/preview は動く
  (実運用では adapter-node 等を明示する)
- 特徴デモ (`specialty/+page.server.ts` + `+page.svelte`、フォームアクション) は**測定後に追加**。
  JS を切った実ブラウザで ★0→★1 を確認済み (素のフォーム POST をサーバが処理して再描画)
