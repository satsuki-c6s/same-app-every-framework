# 測定結果

**測定は1実装につき1回だけ** (`scripts/measure.sh`、やり直しなし)。
章をまたぐ数字は測定日が異なるため、比較するときはその旨を明示する。

## 測定条件 (全実装共通)

- Windows 11 / Node v25.2.1 / npm (すべての実装で同一マシン・同一バージョン)
- 測定の直前に動作確認 (install + build + 画面操作) を1度行う。
  したがって **npm キャッシュは全実装とも温まった状態**で測る (条件を揃えるため)
- `install` はクリーンな状態 (node_modules / dist 削除後) から
- `dev-until-http200` は起動コマンド実行から HTTP 200 が返るまでの壁時計。
  ツール自身が出す "ready in" 表示は使わない

## 第1章: 土台

### vite (vanilla-ts) — 測定日 2026-08-01

| 指標 | 値 |
|---|---|
| install (キャッシュ温) | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 45 |
| 本番ビルド (`tsc && vite build`) | 1,491 ms |
| JS 転送量 (gzip 合計) | **2,479 B** |
| dev サーバ起動 → HTTP 200 | 1,177 ms |
| 公式スキャフォールドの行数 (vanilla-ts) | 417 行 |
| 共通デモの実装行数 (main.ts + index.html) | 120 行 |

備考:

- フレームワークなしの**基準値**。JS 2.5KB は「この画面を素で書くとこの重さ」という下限で、
  以降の各フレームワークの数字はこれとの差分が「フレームワーク本体の重さ」になる
- 素の実装では、入力欄を作り直すとフォーカスが飛ぶため「骨組みは一度だけ描き、
  変わる部分だけ書き換える」処理を自分で書く必要があった (main.ts 参照)。
  この手間が、以降の回でフレームワークが解決する問題そのもの

## 第2章: React 以外の三択

### vue (Vue 3 / create-vue --typescript) — 測定日 2026-08-01

| 指標 | 値 | 基準値 (vite vanilla-ts) |
|---|---|---|
| install (キャッシュ温) | 2,036 ms | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 179 | 45 |
| 本番ビルド (`vue-tsc && vite build`) | 1,839 ms | 1,491 ms |
| JS 転送量 (gzip 合計) | **26,345 B** | 2,479 B |
| dev サーバ起動 → HTTP 200 | 1,146 ms | 1,177 ms |
| 公式スキャフォールドの行数 | 573 行 | 417 行 |
| 共通デモの実装行数 | **102 行** | 120 行 |

備考:

- **+約24KB (gzip) が Vue 本体の重さ**。引き換えに、素の版で自分で書いていた画面更新処理
  (update関数・フォーカス管理) が丸ごと消え、実装は102行に減った
- 開発サーバの起動は基準値とほぼ同じ (どちらも Vite の上に乗っているため)
- ビルドが遅いのは型検査 (vue-tsc) を含むため。スキャフォールドの build スクリプトのまま測った
- 特徴デモ (`vue/specialty.html`) は測定後に追加した別エントリで、**測定値には含まれない**
  (index エントリのバンドルからは参照されない)

### angular (Angular CLI `ng new --defaults`) — 測定日 2026-08-01

| 指標 | 値 | Vue 3 | 基準値 (素のTS) |
|---|---|---|---|
| install (キャッシュ温) | 8,460 ms | 2,036 ms | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 496 | 179 | 45 |
| 本番ビルド (`ng build`) | 2,413 ms | 1,839 ms | 1,491 ms |
| JS 転送量 (gzip 合計) | **65,271 B** | 26,345 B | 2,479 B |
| dev サーバ起動 → HTTP 200 (`ng serve`) | 2,168 ms | 1,146 ms | 1,177 ms |
| 公式スキャフォールドの行数 (lockfile 除く) | 642 行 | 573 行 | 417 行 |
| 共通デモの実装行数 | 112 行 | 102 行 | 120 行 |

備考:

- 公式既定 (`--defaults`) のまま測った。**既定でルータが入る**のが Angular らしさで、
  未使用でも構成に含まれる (全部入りの設計思想)。外さずにそのまま測定した
- 実装は standalone コンポーネント + signals (現行公式のモダン記法)
- 特徴デモ (型付きリアクティブフォーム、`/specialty`) は**測定後に追加**。
  動的 import の別チャンクで、共通デモ側の測定値は追加前のものが正
- dev サーバは Vite 系ではなく Angular CLI (`ng serve`)。それでも 2.2 秒で表示された
