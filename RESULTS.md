# 測定結果

**測定は1実装につき1回だけ** (`scripts/measure.sh`、やり直しなし)。
章をまたぐ数字は測定日が異なるため、比較するときはその旨を明示する。

## 測定条件 (全実装共通)

- Windows 11 / Node v25.2.1 / npm (すべての実装で同一マシン・同一バージョン)
- 測定の直前に動作確認 (install + build + 画面操作) を1度行う。
  したがって **npm キャッシュは全実装とも温まった状態**で測る (条件を揃えるため)
- `install` はクリーンな状態 (node_modules / dist 削除後) から
- `dev-until-http200` は起動コマンド実行から HTTP 200 が返るまでの壁時計。
  ツール自身が出す "ready in" 表示は使わない

## 第1章: 土台

### vite (vanilla-ts) — 測定日 2026-08-01

| 指標 | 値 |
|---|---|
| install (キャッシュ温) | 1,031 ms |
| 依存パッケージ数 (lockfile 実体) | 45 |
| 本番ビルド (`tsc && vite build`) | 1,491 ms |
| JS 転送量 (gzip 合計) | **2,479 B** |
| dev サーバ起動 → HTTP 200 | 1,177 ms |
| 公式スキャフォールドの行数 (vanilla-ts) | 417 行 |
| 共通デモの実装行数 (main.ts + index.html) | 120 行 |

備考:

- フレームワークなしの**基準値**。JS 2.5KB は「この画面を素で書くとこの重さ」という下限で、
  以降の各フレームワークの数字はこれとの差分が「フレームワーク本体の重さ」になる
- 素の実装では、入力欄を作り直すとフォーカスが飛ぶため「骨組みは一度だけ描き、
  変わる部分だけ書き換える」処理を自分で書く必要があった (main.ts 参照)。
  この手間が、以降の回でフレームワークが解決する問題そのもの
